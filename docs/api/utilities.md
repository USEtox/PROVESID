# Utilities

The candidate records and consensus vote behind `Search` (`provesid.tools`),
and small helpers: CAS check digits and the per-user data and cache
directories (`provesid.utils`).

::: provesid.tools

::: provesid.utils
